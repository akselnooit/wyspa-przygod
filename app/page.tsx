import Link from "next/link";
import { Map } from "@/components/Map";
import { characters, locations, sortedAdventures } from "@/lib/content";
import { formatDate } from "@/lib/dates";
export default function Home(){const latest=sortedAdventures()[0]; return <><section className="home-intro"><div className="shell home-grid"><div><p className="eyebrow">Rodzinna kronika odkrywców</p><h1>Wyspa<br/><em>Przygód</em></h1><p className="lead">Witaj w naszej krainie! Tutaj każda ścieżka pamięta wspólną opowieść, a mapa rośnie razem z nami.</p><div className="home-actions"><Link className="button primary" href="/mapa">Otwórz mapę <span>→</span></Link><Link className="text-link" href="/przygody">Czytaj kronikę</Link></div></div><div className="map-preview"><Map/></div></div></section><section className="shell dashboard"><article className="latest"><p className="eyebrow">Ostatnia przygoda</p><p className="date">{formatDate(latest.date)}</p><h2>{latest.title}</h2><p>{latest.description}</p><Link className="text-link" href={`/przygody/${latest.slug}`}>Otwórz wpis <span>→</span></Link></article><div className="stats"><div><strong>{locations.filter(l=>l.visited).length}</strong><span>odkryte miejsca</span></div><div><strong>{characters.length}</strong><span>bohaterów</span></div><nav aria-label="Skróty"><Link href="/miejsca">Poznaj miejsca <span>→</span></Link><Link href="/bohaterowie">Spotkaj bohaterów <span>→</span></Link></nav></div></section></>}
