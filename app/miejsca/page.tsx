import Link from "next/link"; import { SectionTitle } from "@/components/SectionTitle"; import { locations } from "@/lib/content"; import { assetPath } from "@/lib/paths";
export const metadata={title:"Miejsca"};
export default function Page(){return <div className="shell page"><SectionTitle eyebrow="Atlas wyspy" title="Miejsca" intro="Zakątki już poznane i te, które dopiero pojawią się w naszych opowieściach."/><div className="card-grid">{locations.map(l=><Link className="picture-card" href={`/miejsca/${l.slug}`} key={l.slug}><img src={assetPath(l.image)} alt=""/><div><span className={`status ${l.visited?"visited":"unknown"}`}>{l.visited?"Odwiedzone":"Nieodkryte"}</span><h2>Miejsce {l.number} · {l.name}</h2><p>{l.description}</p><span className="card-link">Otwórz kartę →</span></div></Link>)}</div></div>}
